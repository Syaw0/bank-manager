import React from "react";
import Flex from "../../../styles/styledComponents/flex";

const AddCustomer = () => {
  return <Flex data-testid="addCustomer-route">add AddCustomer</Flex>;
};

export default AddCustomer;
