import React from "react";
import Flex from "../../../styles/styledComponents/flex";

const Managers = () => {
  return <Flex data-testid="managers-route">managers ls</Flex>;
};

export default Managers;
