import React from "react";
import Flex from "../../../styles/styledComponents/flex";

const MakeTransaction = () => {
  return <Flex data-testid="makeTransaction-route">make MakeTransaction</Flex>;
};

export default MakeTransaction;
