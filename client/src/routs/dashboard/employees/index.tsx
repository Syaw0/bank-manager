import React from "react";
import Flex from "../../../styles/styledComponents/flex";

const Employees = () => {
  return <Flex data-testid="employees-route">Employees ls</Flex>;
};

export default Employees;
