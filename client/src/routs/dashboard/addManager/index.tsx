import React from "react";
import Flex from "../../../styles/styledComponents/flex";

const AddManager = () => {
  return <Flex data-testid="addManager-route">add AddManager</Flex>;
};

export default AddManager;
